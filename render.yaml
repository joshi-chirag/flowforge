databases:
  - name: flowforge-db
    databaseName: flowforge
    user: flowforge
    plan: free

services:

  # 2. Broker & Cache (Redis)
  - type: keyvalue
    name: flowforge-redis
    plan: free
    ipAllowList: [] # Access isolated only to Render services

  # 3. Daphne ASGI Web Server + Celery Background Worker + Celery Beat Scheduler
  - type: web
    name: flowforge-web
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate
    startCommand: celery -A flowforge worker --loglevel=info --concurrency=1 & celery -A flowforge beat --loglevel=info --scheduler django_celery_beat.schedulers:DatabaseScheduler & daphne -b 0.0.0.0 -p $PORT flowforge.asgi:application
    envVars:
      - key: DEBUG
        value: "False"
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: flowforge-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: keyvalue
          name: flowforge-redis
          property: connectionString
      - key: CELERY_BROKER_URL
        fromService:
          type: keyvalue
          name: flowforge-redis
          property: connectionString
      - key: CELERY_RESULT_BACKEND
        fromService:
          type: keyvalue
          name: flowforge-redis
          property: connectionString
      - key: OPENAI_API_KEY
        sync: false # Set manually in the Render dashboard
